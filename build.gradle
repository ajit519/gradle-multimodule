buildscript {
    repositories {
            jcenter()
	        }
		    dependencies {
		            classpath "net.saliman:gradle-cobertura-plugin:2.3.2"
			        }
				}

				allprojects {
				    group 'com.diaku'
				        version '1.0-SNAPSHOT'

					    apply plugin: 'java'

					        test {
						        exclude 'acceptance/**'
							    }
							    }
